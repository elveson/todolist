import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonTask: {
    backgroundColor: '#1f1e25',
    padding: 15,
    borderRadius: 22,
    marginHorizontal: 20,
    marginBottom: 10,
  },
  textTask: {
    color: 'white',
    fontSize: 20,
  },
});
